module.exports = (name, camelCaseName) => {
  return (
`import withStyles from '@material-ui/core/styles/withStyles';
import * as React from 'react';
import {
  StyleProps,
  stylesCallback,
} from './${name}.styles';
import { Props } from './${name}.types';

export interface State {
}

class ${camelCaseName}View extends
  React.PureComponent<Props & StyleProps, State> {
  public state: State = {
  };

  public render(): JSX.Element {
    const { classes } = this.props;

    return (
      <div className={classes.root}>
        <div className={classes.content}>
          that's right, blue text on red background !
          (generated by ${camelCaseName}View)
        </div>
      </div>
    );
  }
}

export const ${camelCaseName}ViewStyled =
  withStyles(stylesCallback)(${camelCaseName}View);
`)
}
